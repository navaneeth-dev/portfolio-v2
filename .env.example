# Runtime configuration
HCAPTCHA_SECRET=
AWS_REGION=ap-south-1
# Vercel OIDC: assumes this role instead of using the default AWS credential chain.
AWS_ROLE_ARN=
# Local OIDC token from `vercel env pull .env`; refresh it when it expires.
VERCEL_OIDC_TOKEN=
